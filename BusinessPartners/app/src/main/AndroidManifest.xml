<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sap.epm" >

    <!-- allow connections to Internet Services. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".app.CentralActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".app.BusinessPartnerActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".app.BusinessPartnerListActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".app.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.SalesOrderListActivity"
            android:label="@string/title_activity_sales_order" >
        </activity>
        <activity
            android:name=".app.SalesOrderItemsListActivity"
            android:label="@string/title_activity_sales_order_items_list" >
        </activity>
    </application>

</manifest>

